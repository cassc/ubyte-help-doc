{:title "UKeep交易是如何实现的？", :idx 49}
与Ukeep连接的电脑客户端（即浏览器）首先生成原始交易。因为客户端不存储私钥，所有无法对交易签名。客户端必须将原始交易传输给UKeep，请求对其进行签名。UKeep收到请求后，将显示详细交易信息并提示用户通过按钮进行手动确认。如果用户从Ukeep设备端取消，交易即结束。如果用户确认，交易就会被签名并返回给客户端，客户端将签名后的交易广播至区块链，交易即完成。